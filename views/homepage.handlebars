<!--enter div components here-->

{{#each posts as |post|}}
  <div class="post">
    <section>
      <h2>
        <a href="/post/{{post.id}}">{{title}}</a>
      </h2>
    </section>
  </div>
{{/each}}